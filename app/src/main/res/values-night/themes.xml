<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ZappTaxTest" parent="Theme.Material3.DayNight">
        <!-- Customize your dark theme here. -->
         <item name="colorPrimary">@color/primary_yellow</item>
        <item name="android:statusBarColor">@color/yellow_title_background</item>
        <item name="colorPrimaryDark">@color/yellow_title_background</item>
        <item name="colorAccent">@color/yellow_title_background</item>
    </style>
</resources>